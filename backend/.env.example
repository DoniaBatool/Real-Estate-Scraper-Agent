# ============================================================
# ARIA Real Estate Agent — Backend Environment Variables
# Copy this file to backend/.env and fill in your values
# ============================================================

# ── OpenAI (REQUIRED) ─────────────────────────────────────────────────────
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# ── Supabase — Chat History Only (REQUIRED) ───────────────────────────────
# Get from: https://supabase.com → your project → Settings → API
# After creating project, run backend/database/schema.sql in the SQL Editor
DATABASE_URL=postgresql+asyncpg://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
SUPABASE_KEY=eyJ...

# ── Frontend URL — ARIA calls Stagehand via Next.js API routes ─────────────
# Local development:
FRONTEND_URL=http://localhost:3000
# Production (replace with your Vercel URL after deploy):
# FRONTEND_URL=https://your-aria-app.vercel.app

# ── Apify — Agency Discovery (PRIMARY) ───────────────────────────────────
# Used to find real estate agency websites via Google Search Scraper
# Get from: https://console.apify.com → Settings → API & Integrations
APIFY_API_KEY=apify_api_...

# ── Tavily — Web Search (FALLBACK) ───────────────────────────────────────
# Fallback when Apify is unavailable. Also used for general web_search tool.
# Get from: https://tavily.com (free tier: 1000 searches/mo)
TAVILY_API_KEY=tvly-...

# ── ARIA Agent Config ─────────────────────────────────────────────────────
USE_ARIA_AGENT=true
ARIA_MAX_TOOL_ROUNDS=10

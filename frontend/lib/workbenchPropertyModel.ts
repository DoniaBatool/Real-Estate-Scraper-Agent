// Legacy model — no longer used. Kept as empty stub for build compatibility.
import { extractReferenceFromUrl } from "@/lib/extractReferenceFromUrl";
void extractReferenceFromUrl;

export type ColDef = { key: string; label: string; width?: number };
export const COLUMNS: ColDef[] = [];

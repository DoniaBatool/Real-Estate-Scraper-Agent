// Legacy component — no longer used. Kept as empty stub for build compatibility.
export type MaltaAgencySelectionToggle = (id: string) => void;
export function MaltaAgenciesPanel() { return null; }
export default MaltaAgenciesPanel;
